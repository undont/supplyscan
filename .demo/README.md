# acme-dashboard

Internal analytics dashboard for the Acme team.

## Setup

```bash
npm install
npm start
```

## Scripts

- `npm start` — start the dashboard server
- `npm run build` — build for production
- `npm test` — run the test suite
